{
  "name": "sobra",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w sobra-web",
    "build:web": "npm run build -w sobra-web",
    "dev:mobile": "npm start -w sobra-app",
    "lint": "npm run lint -w sobra-web",
    "type-check": "npm run type-check -w sobra-web"
  }
}
